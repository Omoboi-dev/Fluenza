import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The floating dev badge sits on top of the Learn tab at phone widths.
  devIndicators: false,
};

export default nextConfig;
